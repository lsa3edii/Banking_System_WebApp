@using BankingSystem.PL.ViewModels.Manager
@using System.Security.Claims
@model BranchDetailsViewModel

@{
    ViewData["Title"] = "Branch Details";
}
<style>
    :root {
        --primary-dark: #004890; /* new primary dark */
        --primary-medium: #7885FF; /* new primary medium */
        --primary-light: rgb(219, 226, 239); /* new primary light */
        --secondary-dark: #004890; /* mapped to primary-dark because no exact secondary */
        --secondary-light: rgb(219, 226, 239); /* mapped to primary-light */
        --neutral-1: #7885FF; /* mapped to text-light */
        --accent-1: #004890; /* mapped to primary-dark */
        --accent-2: rgb(221, 230, 237); /* primary-lightest */
        --accent-3: #E9FAEB; /* soft background you used */
        --white: #ffffff; /* stays same */
        /* --btn: #0466c8; */
        /* --btn2: #0353a4; */
        /* --btn-m: #8E99FF; */
        --btn: #FFAB35;
        --btn-m: #ffbf69;
        --header: #000000;
    }

    .btn-search {
        background-color: var(--btn);
        border-color: var(--btn);
        color: white;
    }

        .btn-search:hover {
            background-color: var(--btn-m);
            border-color: var(--btn-m);
            color: white;
        }

    .branch-card {
        border: 2px solid var(--accent-2);
        border-radius: 1rem;
        overflow: hidden;
        transition: transform 0.3s ease;
        background: var(--accent-3);
    }

        .branch-card:hover {
            transform: translateY(-5px);
        }

    .card-header {
        background: linear-gradient(10deg, var(--primary-dark), var(--primary-medium));
        color: var(--accent-3) !important;
        padding: 1.5rem;
        border-bottom: 3px solid var(--accent-1);
    }

    .section-heading {
        color: black;
        border-bottom: 2px solid var(--accent-1);
        padding-bottom: 0.5rem;
        margin-bottom: 1.5rem;
    }

    .form-label {
        color: black;
        font-weight: 600;
    }

    .input-group-text {
        background: var(--accent-2) !important;
        border: none;
        color: var(--primary-dark);
    }

    .form-control:read-only {
        background: var( --accent-3);
        border: 2px solid var(--accent-2);
   
    }

    .edit-btn {
        background: var(--btn);
        color: black;
        border: 2px solid var(--btn);
        transition: all 0.3s ease;
    }

        .edit-btn:hover {
            background: var(--btn-m);
            transform: scale(1.05);
        }

    .entity-card {
        background: var(--white);
        border: 2px solid var(--accent-2);
        border-radius: 0.8rem;
        transition: all 0.3s ease;
    }

        .entity-card:hover {
            box-shadow: 0 8px 16px rgba(0,0,0,0.1);
        }

        .entity-card .card-header {
            background: var(--primary-medium) !important;
            color: var(--white) !important;
            border-radius: 0.8rem 0.8rem 0 0 !important;
        }

    .list-group-item {
        background: var(--accent-3);
        border: 1px solid var(--accent-2);
    }

    .badge {
        background: var(--primary-light) !important;
    }

    .btn-outline-primary {
        border-color: var(--primary-medium);
        color: var(--primary-medium);
    }

    .btn-outline-primary:hover {
        background: var(--primary-medium);
        color: var(--white);
    }

    .table-hover tbody tr:hover {
        background: var(--accent-2);
    }

    @@keyframes slideIn {
        from {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .entity-card {
        animation: slideIn 0.5s ease-out;
    }
</style>



<div class="container-fluid mt-4 px-5">  
    <div class="row justify-content-center">
        <div class="col-12">
            <div class="branch-card w-100">
                <div class="card-header">
                    <h3 class="mb-0 text-center">
                        <i class="bi bi-bank me-2"></i>Branch Details
                    </h3>
                </div>
                <div class="card-body px-4 py-4">

                    <!-- Basic Information Section -->
                    <div class="col-12">
                        <h5 class="section-heading">
                            <i class="bi bi-info-circle me-2"></i>Basic Information
                        </h5>
                    </div>

                    <!-- Branch -->
                    <div class="row">

                        <!-- Branch Name -->
                        <div class="col-md-6">
                            <label asp-for="Name" class="form-label fw-semibold">Branch Name</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light"><i class="bi bi-building"></i></span>
                                <input asp-for="Name" class="form-control rounded-end " readonly />
                            </div>
                            <span asp-validation-for="Name" class="text-danger small"></span>
                        </div>

                        <!-- Location -->
                        <div class="col-md-6">
                            <label asp-for="Location" class="form-label fw-semibold">Location</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light"><i class="bi bi-geo-alt"></i></span>
                                <input asp-for="Location" class="form-control rounded-end " readonly />
                            </div>
                            <span asp-validation-for="Location" class="text-danger small"></span>
                        </div>
                    </div>

                    <!-- Operating Hours Section -->
                    <div class="col-12 mt-4">
                        <h5 class="section-heading">
                            <i class="bi bi-clock me-2"></i>Operating Hours
                        </h5>
                    </div>

                    <!-- Business Hours Form -->
                    <form asp-action="EditBusinessHours" method="post" id="businessHoursForm">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="Timing.BranchId" value="@Model.Id" />

                        <!-- Opening Time -->
                        <div class="row">
                            <div class="col-md-6">
                                <label asp-for="Opens" class="form-label fw-semibold">Opening Time</label>
                                <div class="input-group opening-time">
                                    <span class="input-group-text bg-light"><i class="bi bi-sun"></i></span>
                                    <input asp-for="Opens" type="time" class="form-control rounded-end" readonly id="Opens" name="Timing.Opens" />
                                </div>
                                <span asp-validation-for="Opens" class="text-danger small"></span>
                            </div>

                            <!-- Closing Time -->
                            <div class="col-md-6">
                                <label asp-for="Closes" class="form-label fw-semibold">Closing Time</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light"><i class="bi bi-moon"></i></span>
                                    <input asp-for="Closes" type="time" class="form-control rounded-end" readonly id="Closes" name="Timing.Closes" />
                                </div>
                                <span asp-validation-for="Closes" class="text-danger small"></span>
                            </div>
                        </div>

                        <!-- Edit Business Hours Button -->
                        <div class="col-12 text-center mt-2 py-2">

                            <button type="button" class="btn rounded-pill px-4 edit-btn">
                                <i class="bi bi-pencil-square me-2"></i>
                                <span style="color:black">Edit Business Hours</span>
                            </button>
                        </div>
                    </form>

                    <!-- Related Entities Section -->
                    <div class="col-12 mt-4">
                        <h5 class="section-heading">
                            <i class="bi bi-diagram-3 me-2"></i>Related Entities
                        </h5>
                    </div>

                    <!-- Cards for Related Entities -->
                    <div class="row g-3">
                        <!-- Loans Card -->
                        <div class="col-md-6">
                            <div class="card h-100 border-0 shadow-sm">
                                <div class="card-header bg-light">
                                    <h6 class="mb-0 fw-semibold">
                                        <i class="bi bi-cash-coin me-2"></i>Loans
                                    </h6>
                                </div>
                                <div class="card-body">
                                    @if (Model.Loans != null && Model.Loans.Any())
                                    {
                                        <ul class="list-group list-group-flush small">
                                            @foreach (var loan in Model.Loans.Take(3))
                                            {
                                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                                    <span>@loan.LoanAmount.ToString("C")</span>
                                                    <span class="badge bg-secondary text-dark">@loan.CurrentDebt.ToString("C")</span>
                                                </li>
                                            }
                                        </ul>
                                        <div class="text-end mt-2">
                                            <a asp-controller="ManagerLoan" asp-action="GetAllLoans" class="btn btn-sm btn-secondary">View All (@Model.Loans.Count)</a>
                                        </div>

                                    }
                                    else
                                    {
                                        <p class="text-muted small mb-0">No loans available</p>
                                    }
                                </div>
                            </div>
                        </div>

                        <!-- Customers Card -->
                        <div class="col-md-6">
                            <div class="card h-100 border-0 shadow-sm">
                                <div class="card-header bg-light">
                                    <h6 class="mb-0 fw-semibold">
                                        <i class="bi bi-people me-2"></i>Customers
                                    </h6>
                                </div>
                                <div class="card-body">
                                    @if (Model.Customers != null && Model.Customers.Any())
                                    {
                                        <ul class="list-group list-group-flush small">
                                            @foreach (var customer in Model.Customers.Take(3))
                                            {
                                                <li class="list-group-item">
                                                    <i class="bi bi-person me-2"></i>@customer.FirstName @customer.LastName
                                                </li>
                                            }
                                        </ul>
                                        <div class="text-end mt-2">
                                            <a asp-controller="ManagerCustomer" asp-action="GetAllCustomers" asp-route-id="@User.FindFirst(ClaimTypes.NameIdentifier)?.Value" class="btn btn-sm btn-secondary">View All (@Model.Customers.Count)</a>
                                        </div>

                                    }
                                    else
                                    {
                                        <p class="text-muted small mb-0">No customers available</p>
                                    }
                                </div>
                            </div>
                        </div>

                        <!-- Tellers Card -->
                        <div class="col-md-6">
                            <div class="card h-100 border-0 shadow-sm">
                                <div class="card-header bg-light">
                                    <h6 class="mb-0 fw-semibold">
                                        <i class="bi bi-person-badge me-2"></i>Tellers
                                    </h6>
                                </div>
                                <div class="card-body">
                                    @if (Model.Tellers != null && Model.Tellers.Any())
                                    {
                                        <ul class="list-group list-group-flush small">
                                            @foreach (var teller in Model.Tellers.Take(3))
                                            {
                                                <li class="list-group-item">
                                                    <i class="bi bi-person-vcard me-2"></i>@teller.FirstName @teller.LastName
                                                </li>
                                            }
                                        </ul>

                                        <div class="text-end mt-2">
                                            <a class="btn btn-sm btn-secondary" asp-controller="ManagerTeller" asp-action="GetAllTellers" asp-route-id="@User.FindFirst(ClaimTypes.NameIdentifier)?.Value">View All (@Model.Tellers.Count)</a>
                                        </div>
                                    }
                                    else
                                    {
                                        <p class="text-muted small mb-0">No tellers available</p>
                                    }
                                </div>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="card border-0 shadow-sm">
                                <div class="card-header bg-light">
                                    <h6 class="mb-0 fw-semibold">
                                        <i class="bi bi-piggy-bank me-2"></i>Savings Accounts
                                    </h6>
                                </div>
                                <div class="card-body">
                                    @if (Model.Savings != null && Model.Savings.Any())
                                    {
                                        <div class="table-responsive">
                                            <table class="table table-sm table-hover">
                                                <thead>
                                                    <tr>
                                                        <th>Account</th>
                                                        <th>Currency</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    @foreach (var saving in Model.Savings.Take(5))
                                                    {
                                                        <tr>
                                                            <td>@saving.Currency</td>
                                                            <td>@saving.Balance.ToString("C")</td>
                                                        </tr>
                                                    }
                                                </tbody>
                                            </table>
                                            <div class="text-end mt-2">
                                                <a asp-controller="ManagerSavings" asp-action="GetAllSavings" asp-route-id="@User.FindFirst(ClaimTypes.NameIdentifier)?.Value" class="btn btn-sm btn-secondary">
                                                    View All (@Model.Savings.Count)
                                                </a>
                                            </div>
                                        </div>
                                    }
                                    else
                                    {
                                        <p class="text-muted small mb-0">No savings accounts available</p>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="~/js/EditWorkingTime.js"></script>

}